import { Client, APPLICATION_STREAMS } from '../../types';

// Re-export types for convenience
export type { Client };
export { APPLICATION_STREAMS };

// Mock data removed - using real data from Supabase
export const MOCK_CLIENTS: Client[] = [];
